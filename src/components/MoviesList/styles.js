import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  flatList: {
    borderColor: 'black',
    paddingVertical: 20,
    marginVertical: 10,
    marginTop: 50,
  },
});

export default styles;
