import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  favourite: {
    padding: 10,
    flex: 1,
    textAlign: 'center',
    // width: '60%',
  },
});

export default styles;
